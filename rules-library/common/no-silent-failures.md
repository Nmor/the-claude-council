# No-Silent-Failures Rule — PROMOTED TO THE FLOOR

> Relocated 2026-06-28 to `~/.claude/rules/common/no-silent-failures.md`
> (the always-on Floor) so it is consistently enforced like the other
> Floor rules — it was previously here in the lazy library and therefore
> applied inconsistently, which let silent-failure regressions recur.
>
> See `~/.claude/rules/common/no-silent-failures.md` for the full rule
> (now incl. rule 8 "observable best-effort swallow = metric + alert,
> not just a log" and rule 9 "absence-class failures need proactive
> detection: dead-man alerts + startup effective-config log").
